[pytest]
testpaths = tests
timeout = 60
addopts = -v --tb=short

markers =
    gui: tests requiring a display (xvfb-run on Linux)
    integration: tests requiring real DuckDice credentials (DUCKDICE_COOKIE)
